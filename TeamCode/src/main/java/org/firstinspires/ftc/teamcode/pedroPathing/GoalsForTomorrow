1. Clean up file program names
2. Add jittering robot at end of auto
3. Change password on DS wifi stuffs and DS itself
4. find center structure offset point relative to the robot by finding the
angle at which the robot sees the april tag at
5. use limelight to give current position in pedropath pose (x, y, heading)
6. Cl


8. Create an array inside AutoShootTest and AutoAimingTest of different flywheel speeds
for different positions that can be cycled through with d-pad up or down.

*ONLY DO THIS IF LIMELIGHT DISTANCE ISN'T WORKING

be cautious of inconsistent motor and servo names between teleop and auto files

most auto changes are only in blue structure starting point - ill add them to all autos once it works

create constants file